package io.zhudy.notty.domain

/**
 * 回调支持的方法。
 *
 * @author Kevin Zou (kevinz@weghst.com)
 */
enum class CbMethod {
    /**
     * HTTP GET。
     */
    GET,
    /**
     * HTTP POST。
     */
    POST
}